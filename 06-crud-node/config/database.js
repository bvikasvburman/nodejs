const dbConfig = {
    mysql : {
        local: {
            host: 'localhost',
            user: 'root',
            password:'root',
            database:'crud-node'
        },

        live: {
            host: 'localhost',
            user: 'root',
            password:'root',
            database:'crud-node'
        }
    }
}

module.exports = dbConfig;
